import { BlockItemRenderDirective } from './block-item-render.directive';

describe('BlockItemRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockItemRenderDirective();
    expect(directive).toBeTruthy();
  });
});
