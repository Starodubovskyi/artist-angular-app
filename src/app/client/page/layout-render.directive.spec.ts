import { LayoutRenderDirective } from './layout-render.directive';

describe('LayoutRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutRenderDirective();
    expect(directive).toBeTruthy();
  });
});
